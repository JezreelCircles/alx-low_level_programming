#include <stdio.h>
/**
* main - prints all single digit numbers
* Return: Always 0
*/
int main(void)
{
int numbers;
for (numbers = 0; numbers < 10; numbers++)
	;
printf("%d", numbers);
printf("\n");
return (0);
}
