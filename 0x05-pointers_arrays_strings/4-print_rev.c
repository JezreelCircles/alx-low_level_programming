#include "main.h"
#include <stdio.h>

/**
 * print_rev - prints a string in reverse.
 * @s : the used string reference pointrt
 * Return: 0
 */

void print_rev(char *s)
{

int beginning = 0;

while
(s[beginning]);
beginning++;
while (beginning--)
_putchar(s[beginning]);
_putchar('\n');

}
